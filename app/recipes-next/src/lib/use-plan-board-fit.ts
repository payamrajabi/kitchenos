/**
 * Retained for backward compatibility of exports.
 * The plan board no longer uses a stack/grid layout switch —
 * it always renders the horizontal grid.
 */

/** Sticky meal-type row-label column width. */
export const PLAN_MEALS_COLUMN_PX = 72;

/** Fixed width for each day column in the scrollable grid. */
export const PLAN_DAY_COLUMN_PX = 244;
